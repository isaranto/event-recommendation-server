package db;

public interface DbManager {/*
							 * public boolean authenticate(Profile p);
							 * 
							 * public JSONArray getEvent(Event e);
							 * 
							 * // convert result set to json array public
							 * JSONArray rsToJSONArray(ResultSet rs);
							 * 
							 * // get events that The selected User is going to
							 * attend public JSONArray selectUserEvents(Profile
							 * p) throws Exception;
							 * 
							 * // get events that The selected User is NOT going
							 * to attend public JSONArray
							 * selectUserRejectedEvents(Profile p) throws
							 * SQLException, Exception;
							 * 
							 * // get the list of the users that are going to
							 * attend the event public JSONArray
							 * selectUsersfromEvent(Event e) throws
							 * SQLException;
							 */
}
